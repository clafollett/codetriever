//! Example to index data and show what's stored in PostgreSQL and Qdrant
//!
//! TODO: Update this example to use async job pattern with BackgroundWorker
//! This example is currently disabled pending refactor.

fn main() {
    eprintln!("This example is disabled pending async job pattern refactor.");
    eprintln!("See BackgroundWorker and index_rust_mini_redis_test.rs for the new pattern.");
    std::process::exit(1);
}
